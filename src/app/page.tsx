import Carousel from "@/widgets/Carousel/ui/Carousel"

interface IHomePage {

}

const HomePage = ({}:IHomePage) => {
  return (
    <main>
      <Carousel/>
    </main>
  )
}

export default HomePage